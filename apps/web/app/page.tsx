/* import { getSession } from "@frontend/authentication"; */

export default async function Page(): Promise<JSX.Element> {
  /*   const session = await getSession(); */
  return <div>{/* session?.userData?.email */}</div>;
}
